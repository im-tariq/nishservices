generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://d5575c7de283481faf4faf127657130f40c1474952dc25c143a7bd9df01cd249:sk_L9wTMJRK9zccY5Tef76vP@db.prisma.io:5432/postgres?sslmode=require"
}

model Ticket {
  id        String   @id @default(uuid())
  deptName  String
  deptCode  String
  status    String   @default("pending") // pending, active, completed
  queueNum  String
  createdAt DateTime @default(now())
}
